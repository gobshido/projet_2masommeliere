{% extends 'base.html.twig' %}

{% block title %}Editer Message{% endblock %}

{% block body %}
<div class="d-flex justify-content-between">
    <h1>Edit Contact</h1>
    <a class="admin-link" href="{{ path('contact_index') }}">
        <button class="btn admin-btn" role="button">retour</button>
    </a>
</div>
<div class="text-center m-3">
    {{ include('contact/_form.html.twig', {'button_label': 'Valider'}) }}
</div>
<div class="container">
    <div class="row text-center">
        <div class="col">
            {{ include('contact/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
