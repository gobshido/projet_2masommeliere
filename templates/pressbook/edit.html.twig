{% extends 'base.html.twig' %}

{% block title %}Editer l'article de presse{% endblock %}

{% block body %}
<div class="d-flex justify-content-between">
    <h1>Editer l'article de presse</h1>
    <a class="admin-link" href="{{ path('pressbook_index') }}">
        <button class="btn admin-btn" role="button">retour</button>
    </a>
</div>
<div class="text-center m-3">
    {{ include('pressbook/_form.html.twig', {'button_label': 'Valider'}) }}
</div>
<div class="container">
    <div class="row text-center">
        <div class="col">
            {{ include('pressbook/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
