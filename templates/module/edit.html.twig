{% extends 'base.html.twig' %}

{% block title %}Editer Module{% endblock %}

{% block body %}
<div class="d-flex justify-content-between">
    <h1>Editer Module</h1>
    <a class="admin-link" href="{{ path('module_index') }}">
        <button class="btn admin-btn" role="button">retour</button>
    </a>
</div>
<div class="text-center m-3">
    {{ include('module/_form.html.twig', {'button_label': 'Valider'}) }}
</div>
<div class="container">
    <div class="row text-center">
        <div class="col">
            {{ include('module/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
