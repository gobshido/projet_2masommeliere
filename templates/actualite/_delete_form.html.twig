<form method="post" action="{{ path('actualite_delete', {'id': actualite.id}) }}"
      onsubmit="return confirm('Confirmer la suppression de l\'actualité ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ actualite.id) }}">
    <button class="btn admin-btn">supprimer</button>
</form>

