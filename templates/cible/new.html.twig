{% extends 'base.html.twig' %}

{% block title %}Nouvelle Cible{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between">
        <h1>Créer une Cible</h1>
        <a class="admin-link" href="{{ path('cible_index') }}">
            <button class="btn admin-btn">retour</button>
        </a>
    </div>
    <div class="container">
        <div class="row text-center">
            <div class="col">
                {{ include('cible/_form.html.twig') }}
            </div>
        </div>
    </div>

{% endblock %}
