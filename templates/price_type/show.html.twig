{% extends 'base.html.twig' %}

{% block title %}PriceType{% endblock %}

{% block body %}
    <h1>PriceType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ price_type.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ price_type.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('price_type_index') }}">back to list</a>

    <a href="{{ path('price_type_edit', {'id': price_type.id}) }}">edit</a>

    {{ include('price_type/_delete_form.html.twig') }}
{% endblock %}
