<form method="post" action="{{ path('price_type_delete', {'id': price_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ price_type.id) }}">
    <button class="btn">Delete</button>
</form>
