{% extends 'base.html.twig' %}

{% block title %}Editer Prestation{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between">
        <h1>Editer Prestation</h1>
        <a class="admin-link" href="{{ path('prestation_index') }}">
            <button class="btn admin-btn" role="button">retour</button>
        </a>
    </div>
    <div class="edit-btn">
        {{ include('prestation/_form.html.twig', {'button_label': 'Valider'}) }}

        {{ include('prestation/_delete_form.html.twig') }}
    </div>
{% endblock %}
