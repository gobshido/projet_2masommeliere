{{ form_start(form) }}
<div class="m-3">
    {{ form_label (form.nom, 'Particuliers') }}
    {{ form_widget(form.prices) }}
</div>
<div>
    <button class="btn admin-btn">{{ button_label|default('Valider') }}</button>
</div>
{{ form_end(form) }}
