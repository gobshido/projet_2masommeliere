{% extends 'base.html.twig' %}

{% block title %}Editer Catégorie{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between">
        <h1>Editer Catégorie</h1>
        <a class="admin-link" href="{{ path('categorie_index') }}">
            <button class="btn admin-btn" role="button">retour</button>
        </a>
    </div>
    <div>
        {{ include('categorie/_form.html.twig', {'button_label': 'Valider'}) }}
    </div>
        {{ include('categorie/_delete_form.html.twig') }}

{% endblock %}
