{% extends 'base.html.twig' %}

{% block title %}Nouvelles informations personnelles{% endblock %}

{% block body %}
<div class="d-flex justify-content-between">
    <h1>Créer de nouvelles informations personnelles</h1>
    <a class="admin-link" href="{{ path('contactuser_index') }}">
        <button class="btn admin-btn">retour</button>
    </a>
</div>
<div class="container">
    <div class="row text-center">
        <div class="col">
            {{ include('contactuser/_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
